{
  "name": "proset",
  "description": "the game proset",
  "version": "0.0.1",
  "private": false,
  "dependencies": {
    "coffeelint": "^1.5.2",
    "express": "*",
    "mongodb": "*",
    "mongoose": "*",
    "precommit-hook": "^1.0.2",
    "socket.io": "*"
  },
  "precommit": ["lint"],
  "scripts": {
    "lint": "coffeelint app.coffee"
  }
}
